# Python dependencies for Biological Qubits Atlas v2.0
# Version: 2.0.0

# === CORE (v1.3 existant) ===
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
requests>=2.31.0
PyYAML>=6.0

# === NOUVEAUTÉS v2.0 ===

# Automation (PubMed/FPbase extraction)
biopython>=1.80  # Pour parsing XML PMC
beautifulsoup4>=4.12.0  # Parsing HTML/XML (optionnel)
lxml>=4.9.0  # Parser XML rapide

# Machine Learning (GNN prédiction)
torch>=2.0.0  # PyTorch
torch-geometric>=2.3.0  # Graph Neural Networks
rdkit>=2022.9.1  # Cheminformatics (SMILES → graph)
scikit-learn>=1.3.0  # Métriques, train/test split

# Web Dashboard (génération D3.js)
# Aucune dépendance Python supplémentaire (D3.js via CDN)

# Quality Assurance
pytest>=7.4.0  # Tests unitaires (optionnel)
black>=23.0.0  # Formatage code (optionnel)

# === NOTES D'INSTALLATION ===

# Installation complète (avec ML):
# pip install -r requirements_v2.0.txt

# Installation minimale (sans ML):
# pip install pandas numpy pyarrow requests PyYAML biopython

# Pour PyTorch avec CUDA (GPU):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Pour RDKit (conda recommandé):
# conda install -c conda-forge rdkit

# Pour PyTorch Geometric:
# pip install torch-geometric
# pip install pyg-lib torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.0.0+cu118.html


